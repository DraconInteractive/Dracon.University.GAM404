<Window x:Class="GAM404Exercise1.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GAM404Exercise1"
        mc:Ignorable="d"
        Title="Window1" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="{Binding Path=testString}"/>
        <Label Content="{Binding Path=character.level}" Grid.Row="1"/>
        <Label Content="{Binding Path=character.experiencePercent}" Grid.Row="2"/>
        <Label Content="{Binding Path=character.description}" Grid.Row="3"/>

    </Grid>
</Window>
